# B5 生日派對預算 — 單元完成報告書

> **建立日期**：2026-03-24
> **更新日期**：2026-03-24（第七輪：B5_SCENARIOS easy/normal 8→12 組，共 32 組）
> **更新日期**：2026-03-25（第九輪：超支時智慧移除建議 `_handleConfirm` 超支分支改版）
> **專案名稱**：Money Tutor 金錢教學系統
> **單元編號**：B5 — 生日派對預算（Party Budget）
> **系列**：B 預算規劃
> **報告類型**：單元完成報告（測驗內容 / 教學設計 / 技術規格）

---

## 一、檔案規模總覽

| 檔案 | 路徑 | 行數/版本 |
|------|------|---------|
| HTML | `html/b5_party_budget.html` | — |
| JS | `js/b5_party_budget.js` | ~781 行，v3.7 |
| CSS（專用）| `css/b5_party_budget.css` | ~243 行 |
| 作業單 | `worksheet/units/b5-worksheet.js` | 128 行 |

---

## 二、單元特色

### 2.1 教學定位

B5 訓練「在預算限制下，滿足必要條件後進行最佳選購」的複合預算能力。
學習路徑：**加總計算（B1/B2）→ 比較判斷（B4）→ 限制條件下選購（B5）→ 市場採購（B6）**

### 2.2 必買約束系統（B5 核心特色）

B5 設計了**必買 vs. 選購**的雙層商品結構：

| 商品類型 | 圖示 | 行為 | 教學意義 |
|---------|------|------|---------|
| **必買** | 🔒 鎖定標記 | 初始已選中，無法取消 | 固定支出概念 |
| **選購** | 可自由選取 | 點擊切換選取狀態 | 可變支出決策 |

**必買商品**：生日蛋糕 🎂（380元）、果汁飲料 🧃（120元）— 合計 500 元固定支出

### 2.3 即時預算條（`b5-total-bar`）

動態顯示目前選購金額與預算的關係：

| 狀態 | 條件 | 顏色 | 說明 |
|------|------|------|------|
| `ok` | 總金額 ≤ 90% 預算 | 綠色 | 安全範圍 |
| `near` | 總金額 > 90% 預算 | 黃色 | 接近預算上限 |
| `over` | 總金額 > 預算 | 紅色 | 超支警告 |

**教學設計**：黃色「警戒區」讓學生感受接近限制的壓力，培養預算意識。

### 2.4 商品資料庫（`B5_ALL_ITEMS`）

| id | 商品 | 價格 | 必買 |
|----|------|------|------|
| cake | 生日蛋糕 🎂 | 380 | ✅ |
| drink | 果汁飲料 🧃 | 120 | ✅ |
| balloon | 彩色氣球 🎈 | 50 | — |
| gift | 小禮物 🎁 | 200 | — |
| plate | 派對紙盤 🍽️ | 45 | — |
| candle | 生日蠟燭 🕯️ | 30 | — |
| ribbon | 彩帶裝飾 🎊 | 65 | — |
| hat | 派對帽 🎉 | 80 | — |
| candy | 糖果禮包 🍬 | 90 | — |
| photo | 拍立得相機 📸 | 150 | — |
| popper | 噴彩拉炮 🎆 | 55 | — |
| banner | 生日橫幅 🏷️ | 70 | — |

**固定支出下限**：蛋糕 + 飲料 = 500 元

### 2.5 關卡制結構

**32 組關卡**（easy/normal 各 12 組，hard 8 組；2026-03-24 擴充 easy/normal 各 8→12），每關定義：
- `budget`：本關總預算
- `availableIds`：本關可選商品清單（子集，不含全部 12 件）

### 2.6 智慧移除建議（超支回饋）—— 2026-03-25 新增

超支時，系統自動計算最佳移除建議：在已選的**選購商品**中，找出**最貴且移除後總金額 ≤ 預算**的單一商品，以高亮提示顯示：

```
💸 超出預算了！請重新選擇
  超出 95 元
💡 試試取消勾選「📸 拍立得相機」（150元）！
```

**演算法**（貪婪單步）：

```javascript
const selectedOptionals = g.items
    .filter(i => !i.must && g.selectedIds.has(i.id))
    .sort((a, b) => b.price - a.price);          // 由貴到便宜排序
const suggestion = selectedOptionals
    .find(i => total - i.price <= g.budget);      // 第一個移除後達標的
```

**設計決策**：
- 僅建議**單一**移除（不做多步組合）：簡化認知負擔，符合低年級使用者需求
- 必買商品不進入候選清單（不可移除）
- 若無單一商品能解決超支，不顯示提示（`suggestion` 為 undefined）
- `.b5-removal-hint`（CSS）：淡黃底、琥珀邊框，與 B2/B3/B4 錯答提示風格一致

**教學設計對齊**：源自 B5 超支 → A1/A4 找零步驟提示的「回饋引導」模式，提供具體行動建議而非單純否定。

---

## 三、題庫內容詳解（`B5_SCENARIOS`）

### 3.1 簡單模式（12 關）— 預算寬裕（2026-03-24 擴充 8→12）

| 關 | 預算 | 可選商品（含必買）| 策略 |
|----|------|----------------|------|
| 1 | 700 | 蛋糕+飲料+氣球+帽+蠟燭 | 必買500+任1即可 |
| 2 | 600 | 蛋糕+飲料+彩帶+盤+蠟燭 | 必買500+任1 |
| 3 | 650 | 蛋糕+飲料+氣球+盤+帽 | 必買500+任1 |
| 4 | 750 | 蛋糕+飲料+糖果+帽+蠟燭 | 必買500+任1 |
| 5 | 680 | 蛋糕+飲料+拉炮+蠟燭+氣球 | 必買500+任1 |
| 6 | 720 | 蛋糕+飲料+彩帶+帽+盤 | 必買500+任1 |
| 7 | 600 | 蛋糕+飲料+氣球+蠟燭+盤 | 必買500+任1 |
| 8 | 650 | 蛋糕+飲料+帽+拉炮+蠟燭 | 必買500+任1 |
| 9 | 700 | 蛋糕+飲料+橫幅+相機+蠟燭 | 必買500+蠟燭30即達標 |
| 10 | 650 | 蛋糕+飲料+糖果+拉炮+彩帶 | 必買500+任1 |
| 11 | 720 | 蛋糕+飲料+盤+蠟燭+橫幅 | 必買500+任1~2 |
| 12 | 680 | 蛋糕+飲料+彩帶+氣球+盤 | 必買500+任1 |

**設計重點**：預算餘裕 ≥100，可選商品少（5 件，含必買），重點在「必買概念」建立。

### 3.2 普通模式（12 關）— 思考選購（2026-03-24 擴充 8→12）

| 關 | 預算 | 可選商品數 | 策略挑戰 |
|----|------|----------|---------|
| 1 | 700 | 7 件 | 在 200 元餘裕中選 1~2 件 |
| 2 | 750 | 7 件 | 含昂貴禮物(200)+相機(150)，需取捨 |
| 3 | 800 | 7 件 | 300 元餘裕，選 2 件或 1 件較貴的 |
| 4 | 650 | 7 件 | 只剩 150 元，精算 3 件小商品 |
| 5 | 720 | 7 件 | 含相機(150)，需判斷是否值得 |
| 6 | 780 | 7 件 | 多種組合可行，測試決策能力 |
| 7 | 700 | 7 件 | 含相機(150)，加法判斷 |
| 8 | 760 | 7 件 | 多種組合，強化策略思考 |
| 9 | 730 | 7 件 | 含氣球+相機+糖果+彩帶，精算 2~3 件 |
| 10 | 710 | 7 件 | 含禮物(200)+橫幅(70)+氣球(50)，取捨高價 |
| 11 | 740 | 7 件 | 含糖果+帽+相機+拉炮+彩帶，組合多樣 |
| 12 | 770 | 7 件 | 含氣球+橫幅+彩帶+帽+禮物，高密度選擇 |

### 3.3 困難模式（8 關）— 精算取捨

| 關 | 預算 | 可選商品數 | 策略挑戰 |
|----|------|----------|---------|
| 1 | 700 | 9 件 | 僅餘 200 元，9 件中精選，多選 1 件即超支 |
| 2 | 750 | 9 件 | 含禮物(200)+相機(150)，需嚴格計算 |
| 3 | 720 | 9 件 | 220 元餘裕，9 件中選合適組合 |
| 4~8 | 700~780 | 9~10 件 | 高密度選項，測試精確計算 |

**設計重點**：必買(500)固定，餘裕 150~280 元，但可選商品 9~10 件，多選 1 件就超支。

---

## 四、測驗流程

```
[設定] 難度 × 關卡數 × 重試模式
    ↓
[關卡畫面] renderRound()
    ├── 顯示預算金額（`b5-budget-display`）
    ├── 預選必買商品（locked，selectedIds 預填）
    ├── 渲染可選商品格（點擊切換選取）
    └── 即時更新預算條（ok / near / over）
         ↓
[確認] 按下「確認購買」
    ├── 驗證：mustOk && budgetOk && total > 0
    ├─ 通過 → 語音「太棒了！」→ 顯示「下一關」按鈕
    └─ 失敗（未含必買 or 超支）→ 顯示錯誤說明 + 「再試一次」+ 灰色「跳過」
         ↓
[下一關 or 跳過] → 重複至所有關卡完成 → showResults()
```

---

## 五、語音設計

| 場景 | 語音 |
|------|------|
| 進入關卡 | 「派對預算是 X 元，記得一定要買蛋糕和飲料！」|
| 超支警告 | 「超過預算了！總金額 X 元，超出 Y 元」|
| 未含必買 | 「記得一定要有蛋糕和飲料！」|
| 答對 | 「太棒了！預算內完成，還剩 X 元！」|
| 答錯並跳過 | 「這關有點難，我們看下一關！」|
| 全部完成 | 「完成挑戰！共完成 X 關，用時 Y 分 Z 秒」|

---

## 六、作業單

| 項目 | 說明 |
|------|------|
| 題型 | 看預算 + 商品清單 → 勾選合理選購組合 |
| defaultCount | 5 關（每關配置複雜，不適合大量）|

---

## 七、技術注意事項

- 確認邏輯三條件缺一不可：`mustOk`（含必買）、`budgetOk`（不超支）、`total > 0`（有選商品）
- 必買商品在 `renderRound()` 初始化時預填入 `selectedIds`（Set）
- 即時預算條更新觸發於每次點擊商品後（`_updateBudgetBar()`）
- 「再試一次」重渲染同一關卡（`renderRound(currentRound)`），不推進關卡計數
- 「跳過」推進關卡但不計分（不加入 `correctCount`）

---

## 八、作業單題型詳表（2026-03-24 補充）

| # | 題型名稱 | 說明 | 難度對應 |
|---|---------|------|---------|
| 1 | 勾選合理購物組合 | 給預算 + 商品清單（標示必買），勾選在預算內包含必買的合理組合 | easy/normal |
| 2 | 計算已選金額 | 給選定的商品清單，計算總金額（加法練習）| easy |
| 3 | 判斷是否超支 | 給預算 + 已選商品總金額，判斷是否超過預算 | easy/normal |
| 4 | 計算剩餘預算 | 給預算 + 已選金額，計算剩餘可動用金額 | normal |

> `defaultCount = 5`（每關配置複雜，選購組合需人工驗證，不適合大量生成）。

---

## 九、跨系列特色分析（2026-03-24）

### 9.1 B5 vs C5 夠不夠（同為「預算判斷」類型）

| 特性 | B5 生日派對預算 | C5 夠不夠 |
|------|--------------|---------|
| 核心問題 | 「在預算內選什麼？」| 「我的錢夠不夠買這個？」|
| 商品池 | 固定 12 件（2 必買+10 選購）| 48+ 件（動態生成）|
| 金額策略 | 固定場景（B5_SCENARIOS）| PriceStrategy 動態生成 |
| 難度模態 | 商品數量 + 預算緊張度 | 呈現方式（顯示/隱藏金額）|
| 必買限制 | 有（B5 獨有設計）| 無 |
| 圖片資源 | emoji | emoji + 圖片（C5 有圖示）|

### 9.2 B5 vs A3 麥當勞（同為「多選購物」類型）

| 特性 | B5 生日派對預算 | A3 麥當勞 |
|------|--------------|---------|
| 選購限制 | 預算上限（不可超支）| 指定組合（需符合任務）|
| 必選項目 | 2 件必買（蛋糕+飲料）| 依任務指定（套餐組合）|
| 商品展示 | emoji 圖示 + 名稱 + 價格 | 真實餐點圖片 |
| 自訂商品 | 無 | 有（魔法餐點上傳）|
| 付款環節 | 無（只選購）| 有（付款 → 找零完整流程）|

### 9.3 B5 優化建議

| 優先度 | 建議 | 參照 |
|--------|------|------|
| 高 | B5 商品改用圖片（建立 `images/b5/`，12 張 PNG）| B3 圖片系統 |
| 高 | 計分升級：剩餘預算百分比計分（剩≥30%→20分/≥10%→15分/≤10%→10分），鼓勵精算而非剛好 | F4/F5 差分計分 |
| 中 | 超支時顯示「鬼影標示」：淡紅邊框標示移除哪件最便宜非必買商品可解除超支 | C4 鬼影佔位提示 |
| 中 | 增加商品池至 20 件（加入更多派對用品，如蠟燭顏色/氣球形狀選擇）| C5 48+ 件資料庫 |
| 中 | 加入「預算剩餘顯示動畫」（選商品後即時更新剩餘金額，視覺更直觀）| B5 已有 total-bar，可強化 |
| 低 | 新增「必買外預算計算」獨立顯示（「派對必備已花 500 元，剩餘可選購 X 元」）| A3 套餐計費 |

> **差分計分規格**：詳見 `report/B_Series_Unit_Completion_Report.md §五十九`（F4/F5 計時器 + 差分計分 → B 系列遊戲化升級）。
> **鬼影提示設計規格**：詳見 `report/B_Series_Unit_Completion_Report.md §五十四`（C4 鬼影佔位提示 → B1/B5 視覺輔助設計）。
